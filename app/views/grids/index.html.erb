<p style="color: green"><%= notice %></p>

<div id="grids">
  <% @grids.each do |grid| %>
    <%= render grid %>
    <p>
      <%= link_to "Show this grid", grid %>
    </p>
  <% end %>
</div>
<br/>
<button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full"> <%= link_to "Add New Car", new_grid_path %> </button>
